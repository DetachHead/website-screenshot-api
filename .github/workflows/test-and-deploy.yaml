name: check
on:
    - push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v3
              with:
                  node-version: 19.7.0
            - run: npm ci
            - run: npm run check
              env:
                  CI: true
